# =============================================================================
# v19_thr70_andgate — AND-gate + 中等门槛
# =============================================================================
# Motivation (来自 oracle replay 诊断):
#   v18 全家 (thr=0.45 + AND-gate) oracle ceiling 仅 final_score=0.62, 中位
#   episode 长度 3 步 (vs SFT 4 步). thr=0.45 太松, SFT 早就把目标推到 ≥0.95.
#   → 抬高双轴门槛, 强迫 agent 多走/精修.
#
# 假设:
#   - oracle replay 在此 yaml 下 final_score ≥ 0.80, episode_len ≥ 4
#   - RL ceiling 显著高于 v18_andgate
#
# 相对 v18_andgate 的改动:
#   ★ success_position_threshold: 0.45 -> 0.70
#   ★ success_orientation_threshold: 0.45 -> 0.70
#   ★ near_success_threshold: 0.35 -> 0.55     (跟阈值同步抬)
#   ★ success_score_threshold: 0.40 -> 0.65    (回滚保险, AND 模式忽略)
# =============================================================================

env1:
    env_name: active_spatial
    env_config:
        jsonl_path: "/scratch/by2593/project/Active_Spatial/VAGEN/data_gen/active_spatial_pipeline/output_v2/train_data_0267_840790.jsonl"

        render_backend: local
        gs_root: "/scratch/by2593/project/Active_Spatial/InteriorGS"
        gpu_device: 4
        image_width: 256
        image_height: 256

        step_translation: 0.3
        step_rotation_deg: 20.0

        max_actions_per_step: 5

        action_space: "strafe"
        enable_explicit_done: false

        # ─── γ-potential shaping (沿用 v18_potential2) ──────────────────────
        enable_potential_field: true
        potential_field_position_weight: 0.7
        potential_field_orientation_weight: 0.3
        potential_field_reward_scale: 0.5
        potential_field_progress_mode: "potential"
        potential_field_gamma: 0.99

        success_score_threshold: 0.65          # ★ 0.40 -> 0.65  (AND 模式忽略, 回滚保险)
        enable_auto_termination: true

        near_success_threshold: 0.55           # ★ 0.35 -> 0.55
        near_success_bonus: 0.2
        near_success_mode: "constant"

        # ─── ★ v19_thr70_andgate: 中等 AND-gate ─────────────────────────────
        success_require_both: true
        success_position_threshold: 0.70       # ★ 0.45 -> 0.70
        success_orientation_threshold: 0.70    # ★ 0.45 -> 0.70

        enable_collision_detection: false
        enable_visibility_check: false

        format_reward: 0.05
        invalid_format_penalty: -0.2
        max_consecutive_invalid_actions: 3
        premature_done_penalty: -0.3
        step_penalty: -0.005
        enable_step_penalty: true
        success_reward: 50.0
        max_distance: 5.0

        prompt_format: free_think
        action_sep: "|"
        image_placeholder: "<image>"

        max_episode_steps: 60

    train_size: 259
    test_size: 19
